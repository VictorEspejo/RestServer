const { Router } = require("express");
const { check } = require("express-validator");

const router = Router();

router.get("/", );

router.get("/:id");

router.put(
  "/:id",
);

router.post(
  "/",
);

router.delete(
  "/:id",
);

module.exports = router;
